import { Routes } from '@angular/router';
import { ExchangeRateComponent } from './features/exchange-rate/view/exchange-rate';

export const routes: Routes = [
    { path: '', component: ExchangeRateComponent }
];